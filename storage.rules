rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Each teacher can only read/write their own folder
    match /teachers/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
